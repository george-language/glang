# file debug.glang: tools for debugging objects

# assert that an expression is true (throws an error if false)
# returns null
func assert(expr) {
    if expr != true {
        uhoh("assertion failed, resulting expression was not true");
    }
}
