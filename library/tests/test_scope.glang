# file test_scope.glang: test glang's scope in function definitions

func import() {
    fetch std_math;
}

import();

try {
    math_pi # can't access this, it's in import()
} catch _ {
    skip;
}

bark("test_scope.glang: Successful");
